# Antigravity Auto-Retry Standalone App

独立运行的自动重试应用，提供比 VS Code 扩展更好的体验。

## ✨ 特性

| 功能 | 描述 |
|------|------|
| 🔔 **系统通知** | macOS 原生通知，不错过任何重试 |
| 🎨 **彩色日志** | 终端彩色输出，状态一目了然 |
| 📝 **文件日志** | 自动记录到 `~/.antigravity-retry/` |
| ⌨️ **快捷键控制** | 终端内快捷键操作 |
| 📊 **统计信息** | 运行时间、重试次数统计 |

## 🚀 快速开始

### 安装依赖

```bash
cd standalone-retry
npm install
```

### 运行

```bash
# 默认端口 9222
npm start

# 自定义端口
CDP_PORT=9333 npm start
```

### 前提条件

确保 Antigravity IDE 使用调试端口启动：

```bash
/Applications/Antigravity.app/Contents/MacOS/Electron --remote-debugging-port=9222
```

## ⌨️ 快捷键

在应用运行时，按下以下键：

| 按键 | 功能 |
|------|------|
| `p` | 暂停/恢复 自动重试 |
| `r` | 重置重试计数器 |
| `s` | 显示当前状态 |
| `q` | 退出应用 |
| `Ctrl+C` | 强制退出 |

## ⚙️ 配置

编辑 `index.js` 中的 `CONFIG` 对象：

```javascript
const CONFIG = {
    cdpPort: 9222,              // CDP 端口
    checkInterval: 2000,        // 检查间隔 (ms)
    debounceTime: 3000,         // 防抖时间 (ms)
    maxRetries: 30,             // 最大重试次数 (0 = 无限)
    enableNotifications: true,  // 系统通知
    enableSound: true,          // 通知声音
    logToFile: true,            // 记录到文件
};
```

## 📁 日志位置

日志文件保存在：`~/.antigravity-retry/retry-YYYY-MM-DD.log`

## 🔧 构建为独立应用

```bash
# 安装 pkg
npm install -g pkg

# 构建
npm run build

# 输出在 dist/AntigravityAutoRetry
```

## 📝 与扩展配合使用

这个独立应用**只处理 Auto-Retry 功能**。

要获得完整的自动化体验，你仍然需要：
1. ✅ 安装 `antigravity-auto-accept` 扩展 (处理 Auto-Accept)
2. ✅ 运行这个独立应用 (更好的 Auto-Retry 体验)

可以**禁用扩展中的 Auto-Retry** 功能，只使用独立应用：
- 在 VS Code 设置中: `antigravity-auto-accept.autoRetryEnabled: false`

## 🆚 与扩展内置 Auto-Retry 对比

| 功能 | 扩展内置 | 独立应用 |
|------|----------|----------|
| 系统通知 | ❌ | ✅ |
| 彩色日志 | ❌ | ✅ |
| 文件日志 | ❌ | ✅ |
| 快捷键控制 | 有限 | ✅ 完整 |
| 统计信息 | 基础 | ✅ 详细 |
| 独立运行 | ❌ | ✅ |
| IDE 关闭后继续 | ❌ | ✅ |
